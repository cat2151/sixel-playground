[package]
name = "wave-chart"
version = "0.1.0"
edition = "2021"
description = "Library for generating image data from waveforms and line charts"

[dependencies]
plotters = { version = "0.3.7", default-features = false, features = ["bitmap_backend", "line_series", "full_palette"] }
